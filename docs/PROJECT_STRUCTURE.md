# 论文图像伪造数据集生成项目 - 项目结构文档

## 📋 项目概述

这是一个用于生成论文图像伪造数据集的自动化流水线项目，主要功能包括：
- 使用 SAM (Segment Anything Model) 生成图像掩码
- 生成挖洞图（corrupted images）
- 使用 RePaint 扩散模型生成伪造图像
- 完整的质量评估（QA）系统
- 元数据管理和失败追踪

---

## 📁 项目目录结构

```
paper-fig-forgery-dataset/
│
├── 📂 checkpoints/                    # 模型权重文件
│   └── sam/                           # SAM 模型检查点
│       ├── sam_vit_b_01ec64.pth      # SAM ViT-B 模型
│       ├── sam_vit_h_4b8939.pth      # SAM ViT-H 模型
│       └── sam_vit_l_0b3195.pth      # SAM ViT-L 模型
│
├── 📂 data/                           # 数据目录（核心）
│   ├── real/                          # 原始真实图像（输入）
│   ├── mask/                          # SAM 生成的掩码图像
│   ├── corrupted/                     # 挖洞图（中间产物）
│   └── fake/                          # 生成的伪造图像（输出）
│       ├── copy_move/                 # 复制-粘贴伪造
│       └── repaint/                   # RePaint 扩散模型伪造
│
├── 📂 docs/                           # 文档目录
│   └── PROJECT_STRUCTURE.md          # 本文档
│
├── 📂 external/                       # 外部依赖项目
│   └── RePaint-main/                  # RePaint 扩散模型项目
│       ├── conf_mgt/                  # 配置管理模块
│       │   ├── __init__.py
│       │   └── conf_base.py
│       ├── confs/                     # RePaint 配置文件
│       │   ├── paper_forgery.yml     # 论文伪造专用配置
│       │   ├── test_inet256_thick.yml # 默认使用的配置
│       │   └── _tmp_pipeline_*.yml   # 临时配置文件
│       ├── data/pretrained/           # 预训练模型
│       ├── guided_diffusion/          # 引导扩散模型核心代码
│       │   ├── gaussian_diffusion.py
│       │   ├── unet.py
│       │   └── ...
│       ├── utils/                     # RePaint 工具函数
│       ├── test.py                    # RePaint 测试脚本
│       └── README.md
│
├── 📂 logs/                           # 日志文件
│   ├── batch_processing_lrs_*.log    # 批处理日志
│   └── batch_processing_mask*.log    # 掩码生成日志
│
├── 📂 meta/                           # 元数据目录
│   ├── meta.jsonl                     # 主元数据文件（JSONL格式）
│   ├── meta.csv                       # 元数据CSV导出
│   └── failed.jsonl                   # 失败记录文件
│
├── 📂 output/                         # 其他输出目录
│
├── 📂 pipeline/                       # 🔥 核心流水线代码
│   │
│   ├── 📂 config/                     # 配置文件目录
│   │
│   ├── 📂 generators/                 # 生成器模块
│   │   ├── copy_move.py              # 复制-粘贴伪造生成器
│   │   ├── repaint_wrapper.py        # RePaint 包装器
│   │   ├── sam_masker.py             # SAM 掩码生成器
│   │   └── splicing.py               # 拼接伪造生成器
│   │
│   ├── 📂 scripts/                    # 辅助脚本
│   │   └── export_meta_csv.py        # 导出元数据为CSV
│   │
│   ├── 📂 steps/                      # 流水线步骤模块
│   │   ├── step_copy_move.py         # 复制-粘贴步骤
│   │   ├── step_corrupted.py         # 生成挖洞图步骤
│   │   ├── step_mask_generate.py     # 掩码生成步骤
│   │   ├── step_mask_qa.py           # 掩码质量评估步骤
│   │   ├── step_sample_qa.py         # 样本质量评估步骤
│   │   └── step.meta.py              # 元数据管理步骤
│   │
│   ├── 📂 utils/                      # 工具函数
│   │   ├── ids.py                    # ID 生成工具
│   │   ├── io.py                     # 输入输出工具
│   │   ├── jsonl.py                  # JSONL 处理工具
│   │   ├── meta_writer.py            # 元数据写入工具
│   │   └── qa.py                     # 质量评估工具
│   │
│   └── run_all.py                     # 🚀 主流水线执行脚本
│
├── 📂 scripts/                        # 独立脚本目录
│   ├── 00_auth/                       # 认证相关
│   │   └── gen_auth.py
│   ├── 01_models/                     # 模型管理
│   │   ├── config.py
│   │   └── list_models.py
│   └── 02_mask/                       # 掩码生成脚本
│       ├── 1-sam_single.py           # 单张图像掩码生成
│       ├── 2-evaluate_mask_quality.py # 掩码质量评估
│       ├── 3-sam_batch.py            # 批量掩码生成
│       └── 4-gen_batch_lrs.py        # 批量生成低分辨率
│
├── 📂 src/                            # 源代码目录（预留）
│
├── README.md                          # 项目说明文档
└── requirements.txt                   # Python 依赖包列表

```

---

## 🔄 核心流水线流程

### 主流水线 (`pipeline/run_all.py`)

```
输入：data/real/*.png（原始图像）
  ↓
步骤1：生成掩码 (SAM)
  → 输出：data/mask/*.png
  → QA：mask_qa（掩码质量评估）
  ↓
步骤2：生成挖洞图
  → 输出：data/corrupted/*.png
  ↓
步骤3：RePaint 修复生成伪造图
  → 输出：data/fake/repaint/*.png
  → QA：fake_qa（伪造图质量评估）
  ↓
输出：meta/meta.jsonl（元数据）
      meta/failed.jsonl（失败记录）
```

### 执行命令示例

```bash
# 基本用法
python pipeline\run_all.py --type repaint --max-len 10 --batch-size 2 --skip-existing

# 参数说明：
# --type repaint          : 使用 RePaint 流水线
# --max-len 10            : 最多处理 10 张图像（0=不限制）
# --batch-size 2          : RePaint 批处理大小
# --skip-existing         : 跳过已存在的输出（推荐）
# --overwrite             : 强制覆盖已有输出（谨慎使用）
# --timestep-respacing 250: RePaint 时间步数（默认250）
```

---

## 📦 核心模块说明

### 1. 生成器模块 (`pipeline/generators/`)

| 文件 | 功能 | 主要类/函数 |
|------|------|------------|
| `sam_masker.py` | SAM 掩码生成 | `initialize_sam_model()`, `get_default_sam_config()` |
| `repaint_wrapper.py` | RePaint 包装器 | `run_repaint_batch()` |
| `copy_move.py` | 复制-粘贴伪造 | 复制-移动算法实现 |
| `splicing.py` | 拼接伪造 | 图像拼接算法 |

### 2. 步骤模块 (`pipeline/steps/`)

| 文件 | 功能 | 输入 | 输出 |
|------|------|------|------|
| `step_mask_generate.py` | 生成掩码 | real 图像 | mask 图像 |
| `step_corrupted.py` | 生成挖洞图 | real + mask | corrupted 图像 |
| `step_copy_move.py` | 复制-粘贴伪造 | real 图像 | fake 图像 |
| `step_mask_qa.py` | 掩码质量评估 | mask 图像 | QA 结果 |
| `step_sample_qa.py` | 样本质量评估 | real + fake | QA 结果 |

### 3. 工具模块 (`pipeline/utils/`)

| 文件 | 功能 |
|------|------|
| `ids.py` | 生成唯一样本 ID |
| `io.py` | 文件输入输出操作 |
| `jsonl.py` | JSONL 格式处理 |
| `meta_writer.py` | 元数据写入（成功/失败记录）|
| `qa.py` | 质量评估函数（`eval_mask_qa()`, `eval_fake_qa()`）|

---

## 📊 元数据系统

### meta.jsonl 格式

每条记录包含：
```json
{
  "sample_id": "唯一样本ID",
  "timestamp": "时间戳",
  "stage": "mask/corrupted/repaint/qa",
  "status": "success/skip/submitted",
  "type": "repaint/copy_move",
  "paths": {
    "real": "原始图像路径",
    "mask": "掩码路径",
    "corrupted": "挖洞图路径",
    "fake": "伪造图路径"
  },
  "params": {
    "模型参数": "..."
  },
  "qa": {
    "mask_qa": {"pass": true, "metrics": {...}},
    "fake_qa": {"pass": true, "metrics": {...}}
  }
}
```

### failed.jsonl 格式

记录失败的样本：
```json
{
  "sample_id": "样本ID",
  "timestamp": "时间戳",
  "stage": "失败阶段",
  "reason": "失败原因",
  "type": "类型",
  "paths": {...},
  "params": {...}
}
```

---

## 🔧 质量评估 (QA) 系统

### Mask QA (`eval_mask_qa()`)

评估指标：
- ✅ **pass**: 是否通过质量检查
- 📏 **mask_ratio**: 掩码占比
- 🔢 **num_regions**: 区域数量
- 📐 **largest_region_ratio**: 最大区域占比
- 其他形态学指标...

### Fake QA (`eval_fake_qa()`)

评估指标：
- ✅ **pass**: 是否通过质量检查
- 📊 **psnr**: 峰值信噪比
- 🎨 **ssim**: 结构相似性
- 🌈 **color_diff**: 颜色差异
- 其他图像质量指标...

---

## 🚀 快速开始

### 1. 环境配置

```bash
# 安装 PyTorch（根据 CUDA 版本）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# 安装依赖
pip install -r requirements.txt

# 验证安装
python -c "import torch; print(torch.cuda.is_available())"
python -c "import cv2; import segment_anything; print('OK')"
```

### 2. 准备数据

```bash
# 将原始图像放入 data/real/ 目录
# 支持格式：.png
```

### 3. 下载模型权重

```bash
# SAM 模型权重放入 checkpoints/sam/
# RePaint 预训练模型放入 external/RePaint-main/data/pretrained/
```

### 4. 运行流水线

```bash
cd E:\GraduationProject\paper-fig-forgery-dataset
python pipeline\run_all.py --type repaint --max-len 5 --batch-size 2 --skip-existing
```

### 5. 查看结果

```bash
# 生成的文件：
# - data/mask/*.png          # 掩码
# - data/corrupted/*.png     # 挖洞图
# - data/fake/repaint/*.png  # 伪造图
# - meta/meta.jsonl          # 元数据
# - meta/failed.jsonl        # 失败记录
```

---

## 📝 关键依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| PyTorch | ≥1.13 | 深度学习框架 |
| OpenCV | ≥4.8.0 | 图像处理 |
| Pillow | ≥10.0.0 | 图像读写 |
| Segment Anything | latest | SAM 模型 |
| NumPy | ≥1.24.0 | 数值计算 |
| PyYAML | ≥6.0 | 配置文件解析 |
| tqdm | ≥4.65.0 | 进度条显示 |

---

## 🎯 项目特点

1. **模块化设计**：每个步骤独立封装，易于维护和扩展
2. **完整的元数据追踪**：记录每个样本的完整生命周期
3. **质量评估系统**：自动过滤低质量样本
4. **断点续跑**：支持 `--skip-existing` 避免重复计算
5. **批量处理**：支持大规模数据集生成
6. **失败追踪**：详细记录失败原因，便于调试

---

## 📌 注意事项

1. **显存要求**：RePaint 需要较大显存，建议 ≥8GB
2. **磁盘空间**：生成的数据集可能占用大量空间
3. **模型权重**：首次运行需下载 SAM 和 RePaint 权重
4. **配置文件**：RePaint 默认使用 `test_inet256_thick.yml`
5. **图像格式**：目前仅支持 PNG 格式

---

## 🔍 故障排查

### 常见问题

1. **CUDA out of memory**
   - 解决：减小 `--batch-size` 参数

2. **找不到 SAM 权重**
   - 解决：检查 `checkpoints/sam/` 目录

3. **RePaint 运行失败**
   - 解决：检查 `external/RePaint-main/confs/` 配置文件

4. **生成的图像质量差**
   - 解决：调整 `--timestep-respacing` 参数（增大值）

---

## 📧 联系方式

如有问题，请查看：
- 项目 README.md
- logs/ 目录下的日志文件
- meta/failed.jsonl 失败记录

---

**文档版本**: v1.0  
**最后更新**: 2026-01-27  
**维护者**: 项目团队




